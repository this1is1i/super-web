# CLAUDE.md

Use [`AGENTS.md`](./AGENTS.md) as the canonical project guide. It documents the current shared rule engine, local AI modes, authoritative WebSocket turn protocol, special board variants, chat, reconnect behavior, and test commands.

Do not duplicate game transitions outside `js/game-rules.js`, and run `npm test` after protocol or rule changes.
